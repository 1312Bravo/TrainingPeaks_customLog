{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: overall",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}/TrainingPeaks_customLog",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/TrainingPeaks_customLog"
            }
        }
    ]
}